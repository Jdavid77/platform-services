apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSetInputProvider
metadata:
  name: opa-app-dev
  namespace: flux-system
  labels:
    app: opa
spec:
  type: Static
  defaultValues:
    version: "3.23.0"
