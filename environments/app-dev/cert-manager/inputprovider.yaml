apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSetInputProvider
metadata:
  name: cert-manager-app-dev
  namespace: flux-system
  labels:
    app: cert-manager
spec:
  type: Static
  defaultValues:
    version: "1.21.0"
